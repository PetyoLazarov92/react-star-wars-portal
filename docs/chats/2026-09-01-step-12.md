# step 12

_Session date: 2026-09-01_

## Prompt 1

> Step 11 is complete.
>
> Let's continue with the next step in the development plan.
>
> Please read the current plan and project instructions, implement the next step only, validate the changes, and update the plan to reflect the actual result.
>
> Keep the implementation simple and consistent with the existing architecture and conventions. Do not move on to the following step.
>
> Do not create or modify any git commits. Only suggest an appropriate Conventional Commit message when the step is complete.
>
> Do not bump the project version unless the changes represent a meaningful release.
>
> Remember not to use em dashes in any project-authored text.

**Actions:**

- Ran `cat package.json`
- Ran `find src -type f | sort`
- Ran `ls -la; echo "---"; cat tsconfig.json 2>/dev/null; echo "---app---"; cat tsconfig.app.json 2>/dev/null; echo "---node---"; cat tsconfig.node.json 2>/dev/null; e …` (multi-line/long command, trimmed)
- Ran `npm view vitest versions --json 2>/dev/null | tail -20; echo "---"; npm view vitest peerDependencies 2>/dev/null; echo "---installed vite---"; cat node_modules/ …` (multi-line/long command, trimmed)
- Ran `npm view vitest version 2>&1; echo "EXIT:$?"`
- Ran `which node; node -v; echo "PATH=$PATH"; ls ~/.nvm/versions/node 2>/dev/null; command -v npm; command -v npx`
- Ran `bash -lc 'which node; which npm; node -v; npm -v' 2>&1`
- Ran `type node 2>&1; echo "---"; node -v 2>&1; echo "---"; npm -v 2>&1`
- Ran `grep -n "nvm\|NVM" ~/.bashrc ~/.zshrc ~/.profile ~/.bash_profile 2>/dev/null; ls -la ~/.nvm 2>/dev/null | head; find / -maxdepth 4 -iname "node" -type f 2>/dev/ …` (multi-line/long command, trimmed)
- Ran `export NVM_DIR="$HOME/.nvm"; [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"; node -v; npm -v; nvm current`
- Ran `export NVM_DIR="$HOME/.nvm"; [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"; npm view vitest version 2>&1; npm view vitest peerDependencies --json 2>&1; npm v …` (multi-line/long command, trimmed)
- Ran `export NVM_DIR="$HOME/.nvm"; [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"; npm view @testing-library/react@16.3.3 peerDependencies --json 2>&1`
- Ran `export NVM_DIR="$HOME/.nvm"; [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"; npm install -D vitest@4.1.11 jsdom@30.0.1 @testing-library/react@16.3.3 @testing- …` (multi-line/long command, trimmed)
- Wrote `vitest.config.ts`
- Wrote `src/test/setup.ts`
- Edited `src/pages/TablePage.tsx`
- Wrote `src/features/auth/loginSchema.test.ts`
- Wrote `src/shared/cache/localStorageCache.test.ts`
- Wrote `src/pages/TablePage.test.tsx`
- Ran `sed -n '1,20p' ./src/features/people/usePeople.ts`
- Wrote `src/features/auth/LoginForm.test.tsx`
- Edited `src/features/auth/LoginForm.test.tsx`
- Edited `tsconfig.node.json`
- Edited `package.json`
- Ran `export NVM_DIR="$HOME/.nvm"; [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"; npm test 2>&1 | tail -100`
- Edited `src/features/auth/LoginForm.test.tsx`
- Ran `export NVM_DIR="$HOME/.nvm"; [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"; npm test 2>&1 | tail -60`
- Ran `export NVM_DIR="$HOME/.nvm"; [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"; npm run typecheck 2>&1 | tail -60`
- Ran `export NVM_DIR="$HOME/.nvm"; [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"; npm run lint 2>&1 | tail -100`
- Wrote `src/features/people/pageParam.ts`
- Edited `src/pages/TablePage.tsx`
- Ran `rm ./src/pages/TablePage.test.tsx`
- Wrote `src/features/people/pageParam.test.ts`
- Ran `export NVM_DIR="$HOME/.nvm"; [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"; set -e; echo "== test =="; npm test 2>&1 | tail -40; echo "== typecheck =="; npm  …` (multi-line/long command, trimmed)
- Ran `export NVM_DIR="$HOME/.nvm"; [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"; echo "== format:check =="; npm run format:check 2>&1 | tail -60`
- Ran `export NVM_DIR="$HOME/.nvm"; [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"; npm run format 2>&1 | tail -20; echo "== build =="; npm run build 2>&1 | tail -60`
- Ran `export NVM_DIR="$HOME/.nvm"; [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"; echo "== test =="; npm test 2>&1 | tail -20; echo "== lint =="; npm run lint 2>&1 …` (multi-line/long command, trimmed)
- Edited `README.md`
- Edited `README.md`
- Edited `README.md`
- Edited `README.md`
- Edited `README.md`
- Edited `README.md`
- Edited `README.md`
- Edited `CHANGELOG.md`
- Edited `docs/development-plan.md`
- Ran `sed -n '1,10p' src/features/auth/loginSchema.ts`
- Edited `src/features/auth/loginSchema.ts`
- Ran `export NVM_DIR="$HOME/.nvm"; [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"; npm test 2>&1 | tail -40`
- Edited `src/features/auth/loginSchema.ts`
- Ran `export NVM_DIR="$HOME/.nvm"; [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"; echo "== test =="; npm test 2>&1 | tail -20; echo "== typecheck =="; npm run type …` (multi-line/long command, trimmed)
- Ran `git status; echo "---"; git diff --stat`

**Response:**

nvm is set up via `.bashrc` but not sourced by non-interactive shells. Let me source it explicitly.

Good, node/npm work once nvm is sourced. Let me check npm registry connectivity to pick a compatible Vitest version.

Now let's set up the Vitest config, a test setup file, and the `npm test` script.

Now let's create the Vitest config, test setup file, and export `parsePage` from TablePage.

Now export `parsePage` from TablePage so it's directly testable:

Now let's write the test files, starting with `loginSchema.test.ts`:

Now `localStorageCache.test.ts`:

Now `TablePage.test.tsx` for the `parsePage` helper:

Good, no top-level side effects. Now the `LoginForm.test.tsx` smoke test:

Now update `tsconfig.node.json` to include `vitest.config.ts`, and add the `npm test` script:

Now let's run the test suite, typecheck, lint, format, and build.

All 27 tests pass. Now let's run typecheck, lint, format, and build.

Typecheck passes cleanly. Now lint:

Now move the test to be next to the new module:

Now re-run the full validation suite: tests, typecheck, lint, format, build.

All clean. Now format check and build:

Prettier reformatted those two files (just whitespace/wrapping), and the build succeeds. Let's re-run the full validation suite once more to confirm everything is green after formatting:

Everything passes: 27 tests, typecheck, lint, format check, and build. Now let's update the README and development plan, then check the CHANGELOG.

Now let me update the README (scripts table, project structure, dependency mention, status), and CHANGELOG.

Now update the project structure diagram in the README:

Now add `test/setup.ts` to the tree and check final README structure diagram:

Now update CHANGELOG's Unreleased section:

Now update the development plan for Step 12: mark it Done, record what was actually built and the `pageParam.ts` extraction decision.

Let me actually perform that boundary-regression check I described, so the plan reflects something verified rather than assumed:

Confirmed: the boundary test catches the regression exactly as expected. Now revert:

Now let's re-run the full validation suite one final time to confirm everything is clean after the revert:

All green. Let's do a final `git status`/diff review before wrapping up:

Step 12 (Testing) is complete and validated. Summary:

**What was built:**
- Added Vitest 4 + React Testing Library 16 (`@testing-library/jest-dom`, `jsdom`) as dev dependencies.
- `vitest.config.ts` (jsdom environment, `src/test/setup.ts` for jest-dom matchers + Testing Library cleanup), `npm test` script, `vitest.config.ts` added to `tsconfig.node.json`.
- 27 tests across 4 files: `loginSchema` boundaries, `localStorageCache` (TTL expiry, corrupted/invalid data, `getStale`), a new `pageParam` helper, and a `LoginForm` enable/disable smoke test.
- Extracted `parsePage` out of `TablePage.tsx` into `src/features/people/pageParam.ts` — required by `eslint-plugin-react-refresh`'s rule that a component file can only export components; documented as a decision in the plan.
- Verified the suite actually catches regressions by temporarily breaking `PASSWORD_MIN`, confirming the test failed, then reverting.
- Updated `README.md`, `CHANGELOG.md`, and `docs/development-plan.md` (Step 12 marked Done).

`npm test`, `npm run typecheck`, `npm run lint`, `npm run format:check`, and `npm run build` all pass.

Suggested commit messages (two logical changes):
- `test: add Vitest + React Testing Library suite for schema, cache, and pagination logic`
- `refactor(people): extract page param parsing into pageParam.ts`

Step 13 (final cleanup & 1.0 readiness) is next, but per your instructions I've stopped here.

## Prompt 2

> Step 12 is complete.
>
> Let's continue with the next step in the development plan.
>
> Please read the current plan and project instructions, implement the next step only, validate the changes, and update the plan to reflect the actual result.
>
> Keep the implementation simple and consistent with the existing architecture and conventions. Do not move on to the following step.
>
> Do not create or modify any git commits. Only suggest an appropriate Conventional Commit message when the step is complete.
>
> Do not bump the project version unless the changes represent a meaningful release.
>
> Remember not to use em dashes in any project-authored text.

**Actions:**

- Ran `grep -rn "TODO\|FIXME\|XXX\|console\.\|debugger" src --include="*.ts" --include="*.tsx" | grep -v ".test."`
- Ran `find src -type f | sort`
- Ran `cat .gitignore; echo "---prettierignore---"; cat .prettierignore; echo "---editorconfig---"; cat .editorconfig`
- Ran `cat docs/chats/README.md 2>/dev/null; echo "---"; ls docs/chats 2>/dev/null; echo "---"; ls docs`
- Ran `grep -c "^" src/*.tsx src/**/*.ts src/**/*.tsx 2>/dev/null | tail -1; echo "---unused exports check via tsc noUnusedLocals already enforced---"; echo done`
- Ran `export NVM_DIR="$HOME/.nvm"; [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"; cd .; rm -rf node_modules; npm ci 2>&1 | tail -30`
- Ran `export NVM_DIR="$HOME/.nvm"; [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"; cd .; echo "== typecheck =="; npm run typecheck 2>&1 | tail -30; echo "== lint == …` (multi-line/long command, trimmed)
- Ran `export NVM_DIR="$HOME/.nvm"; [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"; cd .; (npm run preview -- --port 4321 --strictPort > /tmp/preview.log 2>&1 &) ; s …` (multi-line/long command, trimmed)
- Ran `ls public; file public/favicon.svg 2>/dev/null; head -c 500 public/favicon.svg`
- Ran `export NVM_DIR="$HOME/.nvm"; [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"; node -e ' …` (multi-line/long command, trimmed)
- Wrote `public/favicon.svg`
- Edited `index.html`
- Ran `pkill -f "vite preview" 2>/dev/null; sleep 1; export NVM_DIR="$HOME/.nvm"; [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"; cd .; npm run build 2>&1 | tail -20 …` (multi-line/long command, trimmed)
- Ran `export NVM_DIR="$HOME/.nvm"; [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"; cd .; npm run build 2>&1 | tail -20`
- Ran `cat ./dist/index.html`
- Ran `export NVM_DIR="$HOME/.nvm"; [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"; cd .; echo "== format:check =="; npm run format:check 2>&1 | tail -20; echo "== l …` (multi-line/long command, trimmed)
- Ran `which google-chrome chromium chromium-browser google-chrome-stable 2>/dev/null; ls /usr/bin | grep -i chrom 2>/dev/null; ls /opt 2>/dev/null`
- Ran `export NVM_DIR="$HOME/.nvm"; [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"; cd .; nohup npm run dev -- --port 5175 --strictPort > /tmp/dev.log 2>&1 & echo "s …` (multi-line/long command, trimmed)
- Ran `cd /tmp; for path in "/" "/table"; do …` (multi-line/long command, trimmed)
- Ran `kill %1 2>/dev/null; pkill -f "vite.*5175" 2>/dev/null; true`
- Ran `ps aux | grep -i "vite\|node.*5175" | grep -v grep`
- Ran `cd .; git status`
- Asked: “All 13 planned steps are now complete and every check (typecheck, lint, format, tests, build, a clean npm ci) passes. Should this step cut the 1.0.0 release (bump package.json + move the CHANGELOG's Unreleased entries under a 1.0.0 heading)?”
- Ran `grep -n '"version"' ./package.json ./package-lock.json | head -5`
- Ran `export NVM_DIR="$HOME/.nvm"; [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"; cd .; npm version 1.0.0 --no-git-tag-version 2>&1`
- Edited `CHANGELOG.md`
- Edited `CHANGELOG.md`
- Edited `CHANGELOG.md`
- Edited `README.md`
- Edited `README.md`
- Edited `README.md`
- Edited `README.md`
- Edited `README.md`
- Edited `docs/development-plan.md`
- Ran `export NVM_DIR="$HOME/.nvm"; [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"; cd .; echo "== format:check =="; npm run format:check 2>&1 | tail -30`
- Ran `export NVM_DIR="$HOME/.nvm"; [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"; cd .; echo "== lint =="; npm run lint 2>&1 | tail -30; echo "== typecheck =="; np …` (multi-line/long command, trimmed)
- Ran `cd .; git status; echo "---"; git diff --stat`

**Response:**

No leftover TODOs/console statements. Let's read through each remaining component file I haven't seen yet, to check for dead code/placeholders, and check `App.tsx`, `router.tsx`, `NotFoundPage.tsx`, `people.types.ts`, `people.schema.ts`, `PeopleTable.tsx`, `Pagination.tsx`, `Modal.tsx`, `ThemeToggle.tsx`, `useOnlineStatus.ts`, `useTheme.ts`, `OfflineModal.tsx`.

All look clean, no placeholders. Let's check the remaining feature/shared files.

All clean. Let's check the remaining files: `usePeople.ts`, `Modal.tsx`, `ThemeToggle.tsx`, `useOnlineStatus.ts`, `useTheme.ts`, `OfflineModal.tsx`.

Let's verify the README's "from scratch" setup instructions actually hold up with a clean dependency install, and check for a couple of remaining pieces (`docs/chats/README.md`, unused code via a dedicated dead-code check).

That's fine as-is. Now let's verify a clean install actually works (`npm ci`), then re-run the full validation suite:

Now fix the page title:

Both pages render correctly with the updated title and no console errors. Let's stop the dev server.

Good, the dev server is stopped. Now let's check `git status` for a clean picture of the changes before finalizing docs.

Before finalizing, I want to check one judgment call with you: the plan's own criteria for Step 13 say to cut a `1.0.0` release "if the planned scope is complete and stable," and it now is (all 13 steps done, all checks green). But you've also told me twice not to bump the version unless it's a meaningful release, so I want to confirm rather than assume.

Now let's bump the version and finalize the CHANGELOG.

Version bumped to `1.0.0`. Now update the CHANGELOG to move Unreleased content under a `[1.0.0]` heading:

Now add the Step 13 cleanup entries and fix the footer links:

Now update the "This will grow feature by feature" line and the Versioning section:

Now update the development plan's Step 13 entry to reflect what was actually done:

Now let's run the full validation suite once more after all doc edits:

All green. Let's do a final review of the git diff to make sure everything is coherent.

Step 13 (Final cleanup & 1.0 readiness), the last step in the development plan, is complete.

**What was done:**
- Full re-read of `AGENTS.md`, `README.md`, and the plan against the actual `src/` tree; every file already matched what the docs described, no dead code or TODOs found.
- Found and fixed two genuine leftover Vite scaffold placeholders never touched by any prior step: `index.html`'s `<title>` (was the raw package name) and `public/favicon.svg` (was Vite's default logo) — replaced with `Star Wars Portal` and a small hand-drawn star icon.
- Verified the README's setup instructions truly work from scratch: `rm -rf node_modules && npm ci`, then typecheck/lint/format/test/build all passed with no changes needed.
- Verified in headless Chrome that both `/` and `/table` render correctly with no console errors.
- Asked you whether to cut `1.0.0` given the plan's own trigger condition was met; you confirmed, so `package.json`/`package-lock.json` were bumped via `npm version 1.0.0 --no-git-tag-version` (no git tag/commit created) and `CHANGELOG.md` now has a `[1.0.0]` section with a fresh empty `[Unreleased]` above it.
- Updated `README.md` and `docs/development-plan.md` to reflect the plan being complete.

All 13 planned steps are now `Done`, and `npm run typecheck`, `npm run lint`, `npm run format:check`, `npm test`, and `npm run build` all pass.

Suggested commit messages (two logical changes):
- `chore: fix leftover Vite scaffold title and favicon`
- `chore(release): cut 1.0.0`
